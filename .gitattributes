# Normalize line endings: store LF in the repo, checkout LF everywhere.
# Keeps Prettier, git diffs and CI consistent across Windows/Linux.
* text=auto eol=lf
*.png binary
*.jpg binary
*.webp binary
*.woff2 binary
